# 后端服务监听地址
SERVER_ADDR=:8080

# 允许跨域的前端源，逗号分隔
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# MySQL 连接配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=dynastic

# 启动时自动建表并写入种子数据（首次运行请保持 true）
AUTO_MIGRATE=true
